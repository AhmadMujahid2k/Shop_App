<style>

body{
    font-family: serif
}
h1 {
  background-color: black;
  color: white;
   text-align: center;
}
a:link, a:visited {
  background-color: #ADD8E6;
  color: white;
  padding: 10px 20px;
  text-align: center;
  display: inline-block;
}
a:hover, a:active {
 background-color: blue;
}
table, th,tr{
  border: 1px solid;
  text-align: center;
  height: 70px;
  width: 50%;
}
td{
  border: 1px solid;
  text-align: center;

}

</style>

<%= form_with(model: @shop, local: true) do |form| %>

  <% if @shop.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@shop.errors.count, "error") %> prohibited
        this shop from being saved:
      </h2>
      <ul>
        <% @shop.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :shop_name %><br>
    <%= form.text_field :shop_name %>
  </p>

  <p>
    <%= form.label :shop_description %><br>
    <%= form.text_area :shop_description %>
  </p>

  <p>
    <%= form.submit %>
  </p>
<% end %>


